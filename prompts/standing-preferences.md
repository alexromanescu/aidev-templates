---
category: teamlead
order: 20
description: User's default preferences that the teamlead reads verbatim on every turn (interpolated into the teamlead system prompt). Bullet defaults that should hold across every engagement — examples below; edit in /prompts to fit your workflow.
---
# Standing preferences

The teamlead reads this verbatim on every turn. Add bullets for any
across-engagement defaults you want the teamlead to respect.

- (none yet — add your defaults here, one bullet per preference)

Examples of useful preferences you might add:

```
- Never use `yarn` — this project is npm-only.
- Always run `npm run lint:fix` before committing.
- Prefer named exports over default exports.
- Don't commit `.env.local` or files matching `*.secret`.
- When in doubt about a deferred item, do it on the spot.
```
